---
description: 功能修改前后必须维护项目记忆文件
alwaysApply: true
---

# 项目记忆维护规则

在 `time-investment` 项目中，凡是涉及“功能修改”的任务，必须遵循以下流程：

1. **修改前先读取记忆文件**
   - 必须先读取 `documents/项目记忆.md`，理解当前项目状态、已落地能力和历史决策。

2. **读取后先计划**
   - 在实施修改前，先输出本次修改计划，至少包含：
     - 修改目标
     - 影响范围/文件
     - 风险与验证方式

3. **完成后更新记忆**
   - 功能修改完成后，必须更新 `documents/项目记忆.md`，至少更新：
     - `当前已落地能力（持续维护）`
     - `变更日志（按时间倒序）`

4. **冲突处理**
   - 若本次需求与历史记忆冲突，以用户最新明确指令为准，并在记忆文件中记录冲突与最终决策。
